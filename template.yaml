AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Description: Smart Job Application Tracker - Lambda function

Globals:
  Function:
    Timeout: 15
    MemorySize: 256

Parameters:
  CognitoUserPoolId:
    Type: String
    Description: The Cognito User Pool ID used to authorize API requests
  SNSTopicArn:
    Type: String
    Description: The SNS topic ARN for follow-up reminder notifications

Resources:
  JobTrackerApi:
    Type: AWS::Serverless::Api
    Properties:
      StageName: Prod
      Cors:
        AllowMethods: "'GET,POST,PUT,DELETE,OPTIONS'"
        AllowHeaders: "'Content-Type,Authorization'"
        AllowOrigin: "'*'"
      Auth:
        DefaultAuthorizer: CognitoAuthorizer
        AddDefaultAuthorizerToCorsPreflight: false
        Authorizers:
          CognitoAuthorizer:
            UserPoolArn: !Sub "arn:aws:cognito-idp:${AWS::Region}:${AWS::AccountId}:userpool/${CognitoUserPoolId}"

  JobTrackerFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: backend/
      Handler: app.main.handler
      Runtime: python3.12
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: JobApplications
          S3_RESUME_BUCKET_NAME: smart-job-tracker-resumes-shankar2026
      Events:
        ApiEvent:
          Type: Api
          Properties:
            RestApiId: !Ref JobTrackerApi
            Path: /{proxy+}
            Method: ANY
        HealthCheckEvent:
          Type: Api
          Properties:
            RestApiId: !Ref JobTrackerApi
            Path: /health
            Method: GET
            Auth:
              Authorizer: NONE

  ReminderFunction:
    Type: AWS::Serverless::Function
    Properties:
      CodeUri: backend/
      Handler: app.jobs.reminder_handler.handler
      Runtime: python3.12
      Timeout: 30
      Environment:
        Variables:
          DYNAMODB_TABLE_NAME: JobApplications
          SNS_TOPIC_ARN: !Ref SNSTopicArn
      Events:
        DailySchedule:
          Type: Schedule
          Properties:
            Schedule: "cron(0 8 * * ? *)"
            Description: "Runs daily at 8:00 AM UTC to check for upcoming follow-ups"
            Enabled: true
      Policies:
        - DynamoDBReadPolicy:
            TableName: JobApplications
        - SNSPublishMessagePolicy:
            TopicName: job-tracker-reminders

Outputs:
  ApiUrl:
    Description: "API Gateway endpoint URL"
    Value: !Sub "https://${JobTrackerApi}.execute-api.${AWS::Region}.amazonaws.com/Prod/"